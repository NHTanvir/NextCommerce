apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: nextcommerce
  labels:
    app: api
    app.kubernetes.io/part-of: nextcommerce
spec:
  selector:
    app: api
  ports:
    - port: 3001
      targetPort: 3001
  type: ClusterIP
